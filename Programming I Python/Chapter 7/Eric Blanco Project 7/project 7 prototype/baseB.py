#this is the module that holds functions necessary to analyze baseball data
#inside the functions, convert the data to ints
def bat_avg(h, ab):
    h = int(h)
    ab = int(ab)
    return h / ab

#def onBase():
    #return (hits + BB + HBP) / (AB + BB + HBP + HF)

#def slugging():

#def OPS():

#def runs_prod():

#def

